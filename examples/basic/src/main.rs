use aparan::prelude::*;

#[aparan::main]
async fn main(system: System) {
    // system.add_actor(MyActor {});

    // system.set("rotation", 100);

    // system.schedule(); ??

    // system.start()?;
    system.start();
}
